# HEOR Process Event-Log Fixture v1

This fixture defines a small, deterministic HEOR process-mining contract for
event-log ingestion, pathway discovery summaries, conformance summaries, and
bottleneck summaries.

The fixture is a documented-stage ecosystem contract. It does not add PM4Py or
any other process-mining runtime as a base `innovate` dependency.
