Conductor review remediation for Phase 2 Rust-Native Promotion: added deterministic diagnostics schema round-trip test coverage and tightened simple positive observation schema validation to reject negative observed values. Validation: cargo fmt, cargo clippy --all-targets -D warnings, cargo test --test operations stable_, uv pytest ownership payload suites, pnpm install --frozen-lockfile && pnpm build.
