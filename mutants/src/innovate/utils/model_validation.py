"""Model validation for the Innovate library."""

from typing import Any

import numpy as np

from .validation import validate_float
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore


def validate_bass_parameters(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    args = [params, t_event]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_validate_bass_parameters__mutmut_orig, x_validate_bass_parameters__mutmut_mutants, args, kwargs, None)


def x_validate_bass_parameters__mutmut_orig(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_1(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = None
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_2(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = None

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_3(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = False

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_4(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = None
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_5(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["XXpXX", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_6(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["P", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_7(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "XXqXX", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_8(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "Q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_9(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "XXmXX"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_10(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "M"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_11(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_12(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(None)

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_13(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["XXp_postXX", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_14(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["P_POST", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_15(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "XXq_postXX", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_16(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "Q_POST", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_17(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "XXm_postXX"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_18(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "M_POST"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_19(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = None
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_20(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_21(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(None)
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_22(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = None

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_23(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = True

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_24(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "XXpXX" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_25(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "P" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_26(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" not in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_27(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = None
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_28(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(None, "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_29(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], None, min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_30(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=None, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_31(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=None)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_32(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float("p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_33(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_34(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_35(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, )
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_36(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["XXpXX"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_37(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["P"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_38(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "XXpXX", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_39(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "P", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_40(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=1.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_41(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=2.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_42(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p <= 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_43(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 1:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_44(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(None)
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_45(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = None
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_46(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = True
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_47(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(None)
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_48(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['XXpXX']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_49(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['P']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_50(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = None

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_51(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = True

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_52(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "XXqXX" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_53(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "Q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_54(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" not in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_55(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = None  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_56(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(None, "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_57(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], None, min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_58(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=None, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_59(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=None)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_60(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float("q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_61(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_62(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_63(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, )  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_64(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["XXqXX"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_65(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["Q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_66(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "XXqXX", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_67(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "Q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_68(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=1.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_69(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=11.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_70(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q <= 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_71(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 1:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_72(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(None)
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_73(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = None
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_74(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = True
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_75(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(None)
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_76(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['XXqXX']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_77(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['Q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_78(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = None

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_79(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = True

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_80(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "XXmXX" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_81(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "M" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_82(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" not in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_83(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = None
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_84(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(None, "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_85(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], None, min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_86(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=None)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_87(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float("m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_88(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_89(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", )
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_90(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["XXmXX"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_91(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["M"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_92(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "XXmXX", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_93(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "M", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_94(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=1.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_95(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m < 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_96(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 1:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_97(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(None)
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_98(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = None
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_99(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = True
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_100(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(None)
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_101(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['XXmXX']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_102(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['M']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_103(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = None

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_104(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = True

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_105(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "XXp_postXX" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_106(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "P_POST" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_107(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" not in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_108(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = None
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_109(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(None, "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_110(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], None, min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_111(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=None, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_112(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=None)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_113(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float("p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_114(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_115(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_116(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, )
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_117(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["XXp_postXX"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_118(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["P_POST"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_119(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "XXp_postXX", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_120(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "P_POST", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_121(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=1.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_122(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=2.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_123(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post <= 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_124(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 1:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_125(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    None
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_126(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = None
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_127(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = True
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_128(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(None)
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_129(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['XXp_postXX']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_130(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['P_POST']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_131(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = None

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_132(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = True

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_133(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "XXq_postXX" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_134(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "Q_POST" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_135(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" not in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_136(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = None
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_137(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(None, "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_138(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], None, min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_139(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=None, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_140(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=None)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_141(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float("q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_142(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_143(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_144(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, )
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_145(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["XXq_postXX"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_146(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["Q_POST"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_147(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "XXq_postXX", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_148(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "Q_POST", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_149(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=1.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_150(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=11.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_151(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post <= 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_152(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 1:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_153(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    None
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_154(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = None
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_155(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = True
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_156(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(None)
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_157(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['XXq_postXX']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_158(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['Q_POST']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_159(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = None

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_160(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = True

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_161(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "XXm_postXX" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_162(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "M_POST" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_163(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" not in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_164(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = None
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_165(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(None, "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_166(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], None, min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_167(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=None)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_168(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float("m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_169(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_170(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", )
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_171(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["XXm_postXX"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_172(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["M_POST"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_173(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "XXm_postXX", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_174(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "M_POST", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_175(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=1.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_176(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post < 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_177(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 1:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_178(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(None)
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_179(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = None
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_180(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = True
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_181(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(None)
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_182(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['XXm_postXX']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_183(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['M_POST']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_184(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = None

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_185(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = True

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_186(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params or "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_187(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "XXpXX" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_188(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "P" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_189(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" not in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_190(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "XXqXX" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_191(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "Q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_192(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" not in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_193(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = None
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_194(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["XXpXX"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_195(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["P"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_196(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = None

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_197(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["XXqXX"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_198(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["Q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_199(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = None
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_200(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val * q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_201(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val >= 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_202(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 1 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_203(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float(None)
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_204(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("XXinfXX")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_205(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("INF")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_206(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio >= 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_207(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 2:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_208(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(None)

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_209(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "XXis_validXX": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_210(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "IS_VALID": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_211(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "XXissuesXX": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_212(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "ISSUES": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_213(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "XXrecommended_actionXX": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_214(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "RECOMMENDED_ACTION": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_215(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "XXAdjust parameters to meet validation criteriaXX" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_216(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_217(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "ADJUST PARAMETERS TO MEET VALIDATION CRITERIA" if not is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_218(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if is_valid else "No action needed",
    }


def x_validate_bass_parameters__mutmut_219(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "XXNo action neededXX",
    }


def x_validate_bass_parameters__mutmut_220(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "no action needed",
    }


def x_validate_bass_parameters__mutmut_221(params: dict[str, float], t_event: float | None = None) -> dict[str, Any]:
    """
    Validate Bass model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters
    t_event : Optional[float]
        Time of structural break, if any

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check required parameters
    required_params = ["p", "q", "m"]
    if t_event is not None:
        required_params.extend(["p_post", "q_post", "m_post"])

    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "p" in params:
        try:
            p = validate_float(params["p"], "p", min_val=0.0, max_val=1.0)
            if p < 0:
                issues.append(f"Parameter 'p' (innovation coefficient) should be non-negative, got {p}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p' must be a positive number, got {params['p']}")
            is_valid = False

    if "q" in params:
        try:
            q = validate_float(params["q"], "q", min_val=0.0, max_val=10.0)  # q can be larger than p
            if q < 0:
                issues.append(f"Parameter 'q' (imitation coefficient) should be non-negative, got {q}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q' must be a positive number, got {params['q']}")
            is_valid = False

    if "m" in params:
        try:
            m = validate_float(params["m"], "m", min_val=0.0)
            if m <= 0:
                issues.append(f"Parameter 'm' (market potential) should be positive, got {m}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm' must be a positive number, got {params['m']}")
            is_valid = False

    # Validate post-event parameters if they exist
    if "p_post" in params:
        try:
            p_post = validate_float(params["p_post"], "p_post", min_val=0.0, max_val=1.0)
            if p_post < 0:
                issues.append(
                    f"Parameter 'p_post' (post-event innovation coefficient) should be non-negative, got {p_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'p_post' must be a positive number, got {params['p_post']}")
            is_valid = False

    if "q_post" in params:
        try:
            q_post = validate_float(params["q_post"], "q_post", min_val=0.0, max_val=10.0)
            if q_post < 0:
                issues.append(
                    f"Parameter 'q_post' (post-event imitation coefficient) should be non-negative, got {q_post}"
                )
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'q_post' must be a positive number, got {params['q_post']}")
            is_valid = False

    if "m_post" in params:
        try:
            m_post = validate_float(params["m_post"], "m_post", min_val=0.0)
            if m_post <= 0:
                issues.append(f"Parameter 'm_post' (post-event market potential) should be positive, got {m_post}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'm_post' must be a positive number, got {params['m_post']}")
            is_valid = False

    # Check for reasonable parameter ratios
    if "p" in params and "q" in params:
        p_val = params["p"]
        q_val = params["q"]

        # The ratio p/q affects the timing of the peak adoption rate
        ratio = p_val / q_val if q_val > 0 else float("inf")
        if ratio > 1:  # p > q is unusual but mathematically valid
            issues.append(f"Unusual parameter values: p ({p_val}) > q ({q_val}), typically q > p")

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "NO ACTION NEEDED",
    }

x_validate_bass_parameters__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_validate_bass_parameters__mutmut_1': x_validate_bass_parameters__mutmut_1, 
    'x_validate_bass_parameters__mutmut_2': x_validate_bass_parameters__mutmut_2, 
    'x_validate_bass_parameters__mutmut_3': x_validate_bass_parameters__mutmut_3, 
    'x_validate_bass_parameters__mutmut_4': x_validate_bass_parameters__mutmut_4, 
    'x_validate_bass_parameters__mutmut_5': x_validate_bass_parameters__mutmut_5, 
    'x_validate_bass_parameters__mutmut_6': x_validate_bass_parameters__mutmut_6, 
    'x_validate_bass_parameters__mutmut_7': x_validate_bass_parameters__mutmut_7, 
    'x_validate_bass_parameters__mutmut_8': x_validate_bass_parameters__mutmut_8, 
    'x_validate_bass_parameters__mutmut_9': x_validate_bass_parameters__mutmut_9, 
    'x_validate_bass_parameters__mutmut_10': x_validate_bass_parameters__mutmut_10, 
    'x_validate_bass_parameters__mutmut_11': x_validate_bass_parameters__mutmut_11, 
    'x_validate_bass_parameters__mutmut_12': x_validate_bass_parameters__mutmut_12, 
    'x_validate_bass_parameters__mutmut_13': x_validate_bass_parameters__mutmut_13, 
    'x_validate_bass_parameters__mutmut_14': x_validate_bass_parameters__mutmut_14, 
    'x_validate_bass_parameters__mutmut_15': x_validate_bass_parameters__mutmut_15, 
    'x_validate_bass_parameters__mutmut_16': x_validate_bass_parameters__mutmut_16, 
    'x_validate_bass_parameters__mutmut_17': x_validate_bass_parameters__mutmut_17, 
    'x_validate_bass_parameters__mutmut_18': x_validate_bass_parameters__mutmut_18, 
    'x_validate_bass_parameters__mutmut_19': x_validate_bass_parameters__mutmut_19, 
    'x_validate_bass_parameters__mutmut_20': x_validate_bass_parameters__mutmut_20, 
    'x_validate_bass_parameters__mutmut_21': x_validate_bass_parameters__mutmut_21, 
    'x_validate_bass_parameters__mutmut_22': x_validate_bass_parameters__mutmut_22, 
    'x_validate_bass_parameters__mutmut_23': x_validate_bass_parameters__mutmut_23, 
    'x_validate_bass_parameters__mutmut_24': x_validate_bass_parameters__mutmut_24, 
    'x_validate_bass_parameters__mutmut_25': x_validate_bass_parameters__mutmut_25, 
    'x_validate_bass_parameters__mutmut_26': x_validate_bass_parameters__mutmut_26, 
    'x_validate_bass_parameters__mutmut_27': x_validate_bass_parameters__mutmut_27, 
    'x_validate_bass_parameters__mutmut_28': x_validate_bass_parameters__mutmut_28, 
    'x_validate_bass_parameters__mutmut_29': x_validate_bass_parameters__mutmut_29, 
    'x_validate_bass_parameters__mutmut_30': x_validate_bass_parameters__mutmut_30, 
    'x_validate_bass_parameters__mutmut_31': x_validate_bass_parameters__mutmut_31, 
    'x_validate_bass_parameters__mutmut_32': x_validate_bass_parameters__mutmut_32, 
    'x_validate_bass_parameters__mutmut_33': x_validate_bass_parameters__mutmut_33, 
    'x_validate_bass_parameters__mutmut_34': x_validate_bass_parameters__mutmut_34, 
    'x_validate_bass_parameters__mutmut_35': x_validate_bass_parameters__mutmut_35, 
    'x_validate_bass_parameters__mutmut_36': x_validate_bass_parameters__mutmut_36, 
    'x_validate_bass_parameters__mutmut_37': x_validate_bass_parameters__mutmut_37, 
    'x_validate_bass_parameters__mutmut_38': x_validate_bass_parameters__mutmut_38, 
    'x_validate_bass_parameters__mutmut_39': x_validate_bass_parameters__mutmut_39, 
    'x_validate_bass_parameters__mutmut_40': x_validate_bass_parameters__mutmut_40, 
    'x_validate_bass_parameters__mutmut_41': x_validate_bass_parameters__mutmut_41, 
    'x_validate_bass_parameters__mutmut_42': x_validate_bass_parameters__mutmut_42, 
    'x_validate_bass_parameters__mutmut_43': x_validate_bass_parameters__mutmut_43, 
    'x_validate_bass_parameters__mutmut_44': x_validate_bass_parameters__mutmut_44, 
    'x_validate_bass_parameters__mutmut_45': x_validate_bass_parameters__mutmut_45, 
    'x_validate_bass_parameters__mutmut_46': x_validate_bass_parameters__mutmut_46, 
    'x_validate_bass_parameters__mutmut_47': x_validate_bass_parameters__mutmut_47, 
    'x_validate_bass_parameters__mutmut_48': x_validate_bass_parameters__mutmut_48, 
    'x_validate_bass_parameters__mutmut_49': x_validate_bass_parameters__mutmut_49, 
    'x_validate_bass_parameters__mutmut_50': x_validate_bass_parameters__mutmut_50, 
    'x_validate_bass_parameters__mutmut_51': x_validate_bass_parameters__mutmut_51, 
    'x_validate_bass_parameters__mutmut_52': x_validate_bass_parameters__mutmut_52, 
    'x_validate_bass_parameters__mutmut_53': x_validate_bass_parameters__mutmut_53, 
    'x_validate_bass_parameters__mutmut_54': x_validate_bass_parameters__mutmut_54, 
    'x_validate_bass_parameters__mutmut_55': x_validate_bass_parameters__mutmut_55, 
    'x_validate_bass_parameters__mutmut_56': x_validate_bass_parameters__mutmut_56, 
    'x_validate_bass_parameters__mutmut_57': x_validate_bass_parameters__mutmut_57, 
    'x_validate_bass_parameters__mutmut_58': x_validate_bass_parameters__mutmut_58, 
    'x_validate_bass_parameters__mutmut_59': x_validate_bass_parameters__mutmut_59, 
    'x_validate_bass_parameters__mutmut_60': x_validate_bass_parameters__mutmut_60, 
    'x_validate_bass_parameters__mutmut_61': x_validate_bass_parameters__mutmut_61, 
    'x_validate_bass_parameters__mutmut_62': x_validate_bass_parameters__mutmut_62, 
    'x_validate_bass_parameters__mutmut_63': x_validate_bass_parameters__mutmut_63, 
    'x_validate_bass_parameters__mutmut_64': x_validate_bass_parameters__mutmut_64, 
    'x_validate_bass_parameters__mutmut_65': x_validate_bass_parameters__mutmut_65, 
    'x_validate_bass_parameters__mutmut_66': x_validate_bass_parameters__mutmut_66, 
    'x_validate_bass_parameters__mutmut_67': x_validate_bass_parameters__mutmut_67, 
    'x_validate_bass_parameters__mutmut_68': x_validate_bass_parameters__mutmut_68, 
    'x_validate_bass_parameters__mutmut_69': x_validate_bass_parameters__mutmut_69, 
    'x_validate_bass_parameters__mutmut_70': x_validate_bass_parameters__mutmut_70, 
    'x_validate_bass_parameters__mutmut_71': x_validate_bass_parameters__mutmut_71, 
    'x_validate_bass_parameters__mutmut_72': x_validate_bass_parameters__mutmut_72, 
    'x_validate_bass_parameters__mutmut_73': x_validate_bass_parameters__mutmut_73, 
    'x_validate_bass_parameters__mutmut_74': x_validate_bass_parameters__mutmut_74, 
    'x_validate_bass_parameters__mutmut_75': x_validate_bass_parameters__mutmut_75, 
    'x_validate_bass_parameters__mutmut_76': x_validate_bass_parameters__mutmut_76, 
    'x_validate_bass_parameters__mutmut_77': x_validate_bass_parameters__mutmut_77, 
    'x_validate_bass_parameters__mutmut_78': x_validate_bass_parameters__mutmut_78, 
    'x_validate_bass_parameters__mutmut_79': x_validate_bass_parameters__mutmut_79, 
    'x_validate_bass_parameters__mutmut_80': x_validate_bass_parameters__mutmut_80, 
    'x_validate_bass_parameters__mutmut_81': x_validate_bass_parameters__mutmut_81, 
    'x_validate_bass_parameters__mutmut_82': x_validate_bass_parameters__mutmut_82, 
    'x_validate_bass_parameters__mutmut_83': x_validate_bass_parameters__mutmut_83, 
    'x_validate_bass_parameters__mutmut_84': x_validate_bass_parameters__mutmut_84, 
    'x_validate_bass_parameters__mutmut_85': x_validate_bass_parameters__mutmut_85, 
    'x_validate_bass_parameters__mutmut_86': x_validate_bass_parameters__mutmut_86, 
    'x_validate_bass_parameters__mutmut_87': x_validate_bass_parameters__mutmut_87, 
    'x_validate_bass_parameters__mutmut_88': x_validate_bass_parameters__mutmut_88, 
    'x_validate_bass_parameters__mutmut_89': x_validate_bass_parameters__mutmut_89, 
    'x_validate_bass_parameters__mutmut_90': x_validate_bass_parameters__mutmut_90, 
    'x_validate_bass_parameters__mutmut_91': x_validate_bass_parameters__mutmut_91, 
    'x_validate_bass_parameters__mutmut_92': x_validate_bass_parameters__mutmut_92, 
    'x_validate_bass_parameters__mutmut_93': x_validate_bass_parameters__mutmut_93, 
    'x_validate_bass_parameters__mutmut_94': x_validate_bass_parameters__mutmut_94, 
    'x_validate_bass_parameters__mutmut_95': x_validate_bass_parameters__mutmut_95, 
    'x_validate_bass_parameters__mutmut_96': x_validate_bass_parameters__mutmut_96, 
    'x_validate_bass_parameters__mutmut_97': x_validate_bass_parameters__mutmut_97, 
    'x_validate_bass_parameters__mutmut_98': x_validate_bass_parameters__mutmut_98, 
    'x_validate_bass_parameters__mutmut_99': x_validate_bass_parameters__mutmut_99, 
    'x_validate_bass_parameters__mutmut_100': x_validate_bass_parameters__mutmut_100, 
    'x_validate_bass_parameters__mutmut_101': x_validate_bass_parameters__mutmut_101, 
    'x_validate_bass_parameters__mutmut_102': x_validate_bass_parameters__mutmut_102, 
    'x_validate_bass_parameters__mutmut_103': x_validate_bass_parameters__mutmut_103, 
    'x_validate_bass_parameters__mutmut_104': x_validate_bass_parameters__mutmut_104, 
    'x_validate_bass_parameters__mutmut_105': x_validate_bass_parameters__mutmut_105, 
    'x_validate_bass_parameters__mutmut_106': x_validate_bass_parameters__mutmut_106, 
    'x_validate_bass_parameters__mutmut_107': x_validate_bass_parameters__mutmut_107, 
    'x_validate_bass_parameters__mutmut_108': x_validate_bass_parameters__mutmut_108, 
    'x_validate_bass_parameters__mutmut_109': x_validate_bass_parameters__mutmut_109, 
    'x_validate_bass_parameters__mutmut_110': x_validate_bass_parameters__mutmut_110, 
    'x_validate_bass_parameters__mutmut_111': x_validate_bass_parameters__mutmut_111, 
    'x_validate_bass_parameters__mutmut_112': x_validate_bass_parameters__mutmut_112, 
    'x_validate_bass_parameters__mutmut_113': x_validate_bass_parameters__mutmut_113, 
    'x_validate_bass_parameters__mutmut_114': x_validate_bass_parameters__mutmut_114, 
    'x_validate_bass_parameters__mutmut_115': x_validate_bass_parameters__mutmut_115, 
    'x_validate_bass_parameters__mutmut_116': x_validate_bass_parameters__mutmut_116, 
    'x_validate_bass_parameters__mutmut_117': x_validate_bass_parameters__mutmut_117, 
    'x_validate_bass_parameters__mutmut_118': x_validate_bass_parameters__mutmut_118, 
    'x_validate_bass_parameters__mutmut_119': x_validate_bass_parameters__mutmut_119, 
    'x_validate_bass_parameters__mutmut_120': x_validate_bass_parameters__mutmut_120, 
    'x_validate_bass_parameters__mutmut_121': x_validate_bass_parameters__mutmut_121, 
    'x_validate_bass_parameters__mutmut_122': x_validate_bass_parameters__mutmut_122, 
    'x_validate_bass_parameters__mutmut_123': x_validate_bass_parameters__mutmut_123, 
    'x_validate_bass_parameters__mutmut_124': x_validate_bass_parameters__mutmut_124, 
    'x_validate_bass_parameters__mutmut_125': x_validate_bass_parameters__mutmut_125, 
    'x_validate_bass_parameters__mutmut_126': x_validate_bass_parameters__mutmut_126, 
    'x_validate_bass_parameters__mutmut_127': x_validate_bass_parameters__mutmut_127, 
    'x_validate_bass_parameters__mutmut_128': x_validate_bass_parameters__mutmut_128, 
    'x_validate_bass_parameters__mutmut_129': x_validate_bass_parameters__mutmut_129, 
    'x_validate_bass_parameters__mutmut_130': x_validate_bass_parameters__mutmut_130, 
    'x_validate_bass_parameters__mutmut_131': x_validate_bass_parameters__mutmut_131, 
    'x_validate_bass_parameters__mutmut_132': x_validate_bass_parameters__mutmut_132, 
    'x_validate_bass_parameters__mutmut_133': x_validate_bass_parameters__mutmut_133, 
    'x_validate_bass_parameters__mutmut_134': x_validate_bass_parameters__mutmut_134, 
    'x_validate_bass_parameters__mutmut_135': x_validate_bass_parameters__mutmut_135, 
    'x_validate_bass_parameters__mutmut_136': x_validate_bass_parameters__mutmut_136, 
    'x_validate_bass_parameters__mutmut_137': x_validate_bass_parameters__mutmut_137, 
    'x_validate_bass_parameters__mutmut_138': x_validate_bass_parameters__mutmut_138, 
    'x_validate_bass_parameters__mutmut_139': x_validate_bass_parameters__mutmut_139, 
    'x_validate_bass_parameters__mutmut_140': x_validate_bass_parameters__mutmut_140, 
    'x_validate_bass_parameters__mutmut_141': x_validate_bass_parameters__mutmut_141, 
    'x_validate_bass_parameters__mutmut_142': x_validate_bass_parameters__mutmut_142, 
    'x_validate_bass_parameters__mutmut_143': x_validate_bass_parameters__mutmut_143, 
    'x_validate_bass_parameters__mutmut_144': x_validate_bass_parameters__mutmut_144, 
    'x_validate_bass_parameters__mutmut_145': x_validate_bass_parameters__mutmut_145, 
    'x_validate_bass_parameters__mutmut_146': x_validate_bass_parameters__mutmut_146, 
    'x_validate_bass_parameters__mutmut_147': x_validate_bass_parameters__mutmut_147, 
    'x_validate_bass_parameters__mutmut_148': x_validate_bass_parameters__mutmut_148, 
    'x_validate_bass_parameters__mutmut_149': x_validate_bass_parameters__mutmut_149, 
    'x_validate_bass_parameters__mutmut_150': x_validate_bass_parameters__mutmut_150, 
    'x_validate_bass_parameters__mutmut_151': x_validate_bass_parameters__mutmut_151, 
    'x_validate_bass_parameters__mutmut_152': x_validate_bass_parameters__mutmut_152, 
    'x_validate_bass_parameters__mutmut_153': x_validate_bass_parameters__mutmut_153, 
    'x_validate_bass_parameters__mutmut_154': x_validate_bass_parameters__mutmut_154, 
    'x_validate_bass_parameters__mutmut_155': x_validate_bass_parameters__mutmut_155, 
    'x_validate_bass_parameters__mutmut_156': x_validate_bass_parameters__mutmut_156, 
    'x_validate_bass_parameters__mutmut_157': x_validate_bass_parameters__mutmut_157, 
    'x_validate_bass_parameters__mutmut_158': x_validate_bass_parameters__mutmut_158, 
    'x_validate_bass_parameters__mutmut_159': x_validate_bass_parameters__mutmut_159, 
    'x_validate_bass_parameters__mutmut_160': x_validate_bass_parameters__mutmut_160, 
    'x_validate_bass_parameters__mutmut_161': x_validate_bass_parameters__mutmut_161, 
    'x_validate_bass_parameters__mutmut_162': x_validate_bass_parameters__mutmut_162, 
    'x_validate_bass_parameters__mutmut_163': x_validate_bass_parameters__mutmut_163, 
    'x_validate_bass_parameters__mutmut_164': x_validate_bass_parameters__mutmut_164, 
    'x_validate_bass_parameters__mutmut_165': x_validate_bass_parameters__mutmut_165, 
    'x_validate_bass_parameters__mutmut_166': x_validate_bass_parameters__mutmut_166, 
    'x_validate_bass_parameters__mutmut_167': x_validate_bass_parameters__mutmut_167, 
    'x_validate_bass_parameters__mutmut_168': x_validate_bass_parameters__mutmut_168, 
    'x_validate_bass_parameters__mutmut_169': x_validate_bass_parameters__mutmut_169, 
    'x_validate_bass_parameters__mutmut_170': x_validate_bass_parameters__mutmut_170, 
    'x_validate_bass_parameters__mutmut_171': x_validate_bass_parameters__mutmut_171, 
    'x_validate_bass_parameters__mutmut_172': x_validate_bass_parameters__mutmut_172, 
    'x_validate_bass_parameters__mutmut_173': x_validate_bass_parameters__mutmut_173, 
    'x_validate_bass_parameters__mutmut_174': x_validate_bass_parameters__mutmut_174, 
    'x_validate_bass_parameters__mutmut_175': x_validate_bass_parameters__mutmut_175, 
    'x_validate_bass_parameters__mutmut_176': x_validate_bass_parameters__mutmut_176, 
    'x_validate_bass_parameters__mutmut_177': x_validate_bass_parameters__mutmut_177, 
    'x_validate_bass_parameters__mutmut_178': x_validate_bass_parameters__mutmut_178, 
    'x_validate_bass_parameters__mutmut_179': x_validate_bass_parameters__mutmut_179, 
    'x_validate_bass_parameters__mutmut_180': x_validate_bass_parameters__mutmut_180, 
    'x_validate_bass_parameters__mutmut_181': x_validate_bass_parameters__mutmut_181, 
    'x_validate_bass_parameters__mutmut_182': x_validate_bass_parameters__mutmut_182, 
    'x_validate_bass_parameters__mutmut_183': x_validate_bass_parameters__mutmut_183, 
    'x_validate_bass_parameters__mutmut_184': x_validate_bass_parameters__mutmut_184, 
    'x_validate_bass_parameters__mutmut_185': x_validate_bass_parameters__mutmut_185, 
    'x_validate_bass_parameters__mutmut_186': x_validate_bass_parameters__mutmut_186, 
    'x_validate_bass_parameters__mutmut_187': x_validate_bass_parameters__mutmut_187, 
    'x_validate_bass_parameters__mutmut_188': x_validate_bass_parameters__mutmut_188, 
    'x_validate_bass_parameters__mutmut_189': x_validate_bass_parameters__mutmut_189, 
    'x_validate_bass_parameters__mutmut_190': x_validate_bass_parameters__mutmut_190, 
    'x_validate_bass_parameters__mutmut_191': x_validate_bass_parameters__mutmut_191, 
    'x_validate_bass_parameters__mutmut_192': x_validate_bass_parameters__mutmut_192, 
    'x_validate_bass_parameters__mutmut_193': x_validate_bass_parameters__mutmut_193, 
    'x_validate_bass_parameters__mutmut_194': x_validate_bass_parameters__mutmut_194, 
    'x_validate_bass_parameters__mutmut_195': x_validate_bass_parameters__mutmut_195, 
    'x_validate_bass_parameters__mutmut_196': x_validate_bass_parameters__mutmut_196, 
    'x_validate_bass_parameters__mutmut_197': x_validate_bass_parameters__mutmut_197, 
    'x_validate_bass_parameters__mutmut_198': x_validate_bass_parameters__mutmut_198, 
    'x_validate_bass_parameters__mutmut_199': x_validate_bass_parameters__mutmut_199, 
    'x_validate_bass_parameters__mutmut_200': x_validate_bass_parameters__mutmut_200, 
    'x_validate_bass_parameters__mutmut_201': x_validate_bass_parameters__mutmut_201, 
    'x_validate_bass_parameters__mutmut_202': x_validate_bass_parameters__mutmut_202, 
    'x_validate_bass_parameters__mutmut_203': x_validate_bass_parameters__mutmut_203, 
    'x_validate_bass_parameters__mutmut_204': x_validate_bass_parameters__mutmut_204, 
    'x_validate_bass_parameters__mutmut_205': x_validate_bass_parameters__mutmut_205, 
    'x_validate_bass_parameters__mutmut_206': x_validate_bass_parameters__mutmut_206, 
    'x_validate_bass_parameters__mutmut_207': x_validate_bass_parameters__mutmut_207, 
    'x_validate_bass_parameters__mutmut_208': x_validate_bass_parameters__mutmut_208, 
    'x_validate_bass_parameters__mutmut_209': x_validate_bass_parameters__mutmut_209, 
    'x_validate_bass_parameters__mutmut_210': x_validate_bass_parameters__mutmut_210, 
    'x_validate_bass_parameters__mutmut_211': x_validate_bass_parameters__mutmut_211, 
    'x_validate_bass_parameters__mutmut_212': x_validate_bass_parameters__mutmut_212, 
    'x_validate_bass_parameters__mutmut_213': x_validate_bass_parameters__mutmut_213, 
    'x_validate_bass_parameters__mutmut_214': x_validate_bass_parameters__mutmut_214, 
    'x_validate_bass_parameters__mutmut_215': x_validate_bass_parameters__mutmut_215, 
    'x_validate_bass_parameters__mutmut_216': x_validate_bass_parameters__mutmut_216, 
    'x_validate_bass_parameters__mutmut_217': x_validate_bass_parameters__mutmut_217, 
    'x_validate_bass_parameters__mutmut_218': x_validate_bass_parameters__mutmut_218, 
    'x_validate_bass_parameters__mutmut_219': x_validate_bass_parameters__mutmut_219, 
    'x_validate_bass_parameters__mutmut_220': x_validate_bass_parameters__mutmut_220, 
    'x_validate_bass_parameters__mutmut_221': x_validate_bass_parameters__mutmut_221
}
x_validate_bass_parameters__mutmut_orig.__name__ = 'x_validate_bass_parameters'


def validate_logistic_parameters(params: dict[str, float]) -> dict[str, Any]:
    args = [params]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_validate_logistic_parameters__mutmut_orig, x_validate_logistic_parameters__mutmut_mutants, args, kwargs, None)


def x_validate_logistic_parameters__mutmut_orig(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_1(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = None
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_2(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = None

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_3(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = False

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_4(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = None
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_5(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["XXLXX", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_6(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["l", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_7(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "XXkXX", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_8(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "K", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_9(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "XXx0XX"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_10(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "X0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_11(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = None
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_12(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_13(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(None)
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_14(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = None

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_15(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = True

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_16(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "XXLXX" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_17(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "l" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_18(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" not in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_19(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = None
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_20(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(None, "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_21(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], None, min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_22(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=None)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_23(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float("L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_24(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_25(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", )
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_26(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["XXLXX"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_27(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["l"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_28(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "XXLXX", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_29(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "l", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_30(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=1.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_31(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L < 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_32(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 1:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_33(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(None)
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_34(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = None
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_35(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = True
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_36(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(None)
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_37(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['XXLXX']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_38(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['l']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_39(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = None

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_40(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = True

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_41(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "XXkXX" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_42(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "K" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_43(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" not in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_44(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = None
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_45(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(None, "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_46(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], None, min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_47(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=None)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_48(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float("k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_49(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_50(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", )
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_51(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["XXkXX"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_52(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["K"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_53(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "XXkXX", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_54(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "K", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_55(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=1.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_56(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k < 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_57(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 1:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_58(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(None)
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_59(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = None
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_60(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = True
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_61(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(None)
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_62(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['XXkXX']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_63(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['K']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_64(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = None

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_65(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = True

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_66(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "XXx0XX" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_67(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "X0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_68(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" not in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_69(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(None, "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_70(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], None)
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_71(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float("x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_72(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], )
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_73(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["XXx0XX"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_74(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["X0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_75(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "XXx0XX")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_76(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "X0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_77(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(None)
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_78(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['XXx0XX']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_79(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['X0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_80(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = None

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_81(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = True

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_82(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "XXis_validXX": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_83(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "IS_VALID": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_84(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "XXissuesXX": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_85(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "ISSUES": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_86(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "XXrecommended_actionXX": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_87(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "RECOMMENDED_ACTION": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_88(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "XXAdjust parameters to meet validation criteriaXX" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_89(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_90(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "ADJUST PARAMETERS TO MEET VALIDATION CRITERIA" if not is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_91(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if is_valid else "No action needed",
    }


def x_validate_logistic_parameters__mutmut_92(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "XXNo action neededXX",
    }


def x_validate_logistic_parameters__mutmut_93(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "no action needed",
    }


def x_validate_logistic_parameters__mutmut_94(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Logistic model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["L", "k", "x0"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "L" in params:
        try:
            L = validate_float(params["L"], "L", min_val=0.0)
            if L <= 0:
                issues.append(f"Parameter 'L' (maximum value) should be positive, got {L}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'L' must be a positive number, got {params['L']}")
            is_valid = False

    if "k" in params:
        try:
            k = validate_float(params["k"], "k", min_val=0.0)
            if k <= 0:
                issues.append(f"Parameter 'k' (growth rate) should be positive, got {k}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'k' must be a positive number, got {params['k']}")
            is_valid = False

    if "x0" in params:
        try:
            validate_float(params["x0"], "x0")
        except (ValueError, TypeError):
            issues.append(f"Parameter 'x0' (x-value of sigmoid's midpoint) should be numeric, got {params['x0']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "NO ACTION NEEDED",
    }

x_validate_logistic_parameters__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_validate_logistic_parameters__mutmut_1': x_validate_logistic_parameters__mutmut_1, 
    'x_validate_logistic_parameters__mutmut_2': x_validate_logistic_parameters__mutmut_2, 
    'x_validate_logistic_parameters__mutmut_3': x_validate_logistic_parameters__mutmut_3, 
    'x_validate_logistic_parameters__mutmut_4': x_validate_logistic_parameters__mutmut_4, 
    'x_validate_logistic_parameters__mutmut_5': x_validate_logistic_parameters__mutmut_5, 
    'x_validate_logistic_parameters__mutmut_6': x_validate_logistic_parameters__mutmut_6, 
    'x_validate_logistic_parameters__mutmut_7': x_validate_logistic_parameters__mutmut_7, 
    'x_validate_logistic_parameters__mutmut_8': x_validate_logistic_parameters__mutmut_8, 
    'x_validate_logistic_parameters__mutmut_9': x_validate_logistic_parameters__mutmut_9, 
    'x_validate_logistic_parameters__mutmut_10': x_validate_logistic_parameters__mutmut_10, 
    'x_validate_logistic_parameters__mutmut_11': x_validate_logistic_parameters__mutmut_11, 
    'x_validate_logistic_parameters__mutmut_12': x_validate_logistic_parameters__mutmut_12, 
    'x_validate_logistic_parameters__mutmut_13': x_validate_logistic_parameters__mutmut_13, 
    'x_validate_logistic_parameters__mutmut_14': x_validate_logistic_parameters__mutmut_14, 
    'x_validate_logistic_parameters__mutmut_15': x_validate_logistic_parameters__mutmut_15, 
    'x_validate_logistic_parameters__mutmut_16': x_validate_logistic_parameters__mutmut_16, 
    'x_validate_logistic_parameters__mutmut_17': x_validate_logistic_parameters__mutmut_17, 
    'x_validate_logistic_parameters__mutmut_18': x_validate_logistic_parameters__mutmut_18, 
    'x_validate_logistic_parameters__mutmut_19': x_validate_logistic_parameters__mutmut_19, 
    'x_validate_logistic_parameters__mutmut_20': x_validate_logistic_parameters__mutmut_20, 
    'x_validate_logistic_parameters__mutmut_21': x_validate_logistic_parameters__mutmut_21, 
    'x_validate_logistic_parameters__mutmut_22': x_validate_logistic_parameters__mutmut_22, 
    'x_validate_logistic_parameters__mutmut_23': x_validate_logistic_parameters__mutmut_23, 
    'x_validate_logistic_parameters__mutmut_24': x_validate_logistic_parameters__mutmut_24, 
    'x_validate_logistic_parameters__mutmut_25': x_validate_logistic_parameters__mutmut_25, 
    'x_validate_logistic_parameters__mutmut_26': x_validate_logistic_parameters__mutmut_26, 
    'x_validate_logistic_parameters__mutmut_27': x_validate_logistic_parameters__mutmut_27, 
    'x_validate_logistic_parameters__mutmut_28': x_validate_logistic_parameters__mutmut_28, 
    'x_validate_logistic_parameters__mutmut_29': x_validate_logistic_parameters__mutmut_29, 
    'x_validate_logistic_parameters__mutmut_30': x_validate_logistic_parameters__mutmut_30, 
    'x_validate_logistic_parameters__mutmut_31': x_validate_logistic_parameters__mutmut_31, 
    'x_validate_logistic_parameters__mutmut_32': x_validate_logistic_parameters__mutmut_32, 
    'x_validate_logistic_parameters__mutmut_33': x_validate_logistic_parameters__mutmut_33, 
    'x_validate_logistic_parameters__mutmut_34': x_validate_logistic_parameters__mutmut_34, 
    'x_validate_logistic_parameters__mutmut_35': x_validate_logistic_parameters__mutmut_35, 
    'x_validate_logistic_parameters__mutmut_36': x_validate_logistic_parameters__mutmut_36, 
    'x_validate_logistic_parameters__mutmut_37': x_validate_logistic_parameters__mutmut_37, 
    'x_validate_logistic_parameters__mutmut_38': x_validate_logistic_parameters__mutmut_38, 
    'x_validate_logistic_parameters__mutmut_39': x_validate_logistic_parameters__mutmut_39, 
    'x_validate_logistic_parameters__mutmut_40': x_validate_logistic_parameters__mutmut_40, 
    'x_validate_logistic_parameters__mutmut_41': x_validate_logistic_parameters__mutmut_41, 
    'x_validate_logistic_parameters__mutmut_42': x_validate_logistic_parameters__mutmut_42, 
    'x_validate_logistic_parameters__mutmut_43': x_validate_logistic_parameters__mutmut_43, 
    'x_validate_logistic_parameters__mutmut_44': x_validate_logistic_parameters__mutmut_44, 
    'x_validate_logistic_parameters__mutmut_45': x_validate_logistic_parameters__mutmut_45, 
    'x_validate_logistic_parameters__mutmut_46': x_validate_logistic_parameters__mutmut_46, 
    'x_validate_logistic_parameters__mutmut_47': x_validate_logistic_parameters__mutmut_47, 
    'x_validate_logistic_parameters__mutmut_48': x_validate_logistic_parameters__mutmut_48, 
    'x_validate_logistic_parameters__mutmut_49': x_validate_logistic_parameters__mutmut_49, 
    'x_validate_logistic_parameters__mutmut_50': x_validate_logistic_parameters__mutmut_50, 
    'x_validate_logistic_parameters__mutmut_51': x_validate_logistic_parameters__mutmut_51, 
    'x_validate_logistic_parameters__mutmut_52': x_validate_logistic_parameters__mutmut_52, 
    'x_validate_logistic_parameters__mutmut_53': x_validate_logistic_parameters__mutmut_53, 
    'x_validate_logistic_parameters__mutmut_54': x_validate_logistic_parameters__mutmut_54, 
    'x_validate_logistic_parameters__mutmut_55': x_validate_logistic_parameters__mutmut_55, 
    'x_validate_logistic_parameters__mutmut_56': x_validate_logistic_parameters__mutmut_56, 
    'x_validate_logistic_parameters__mutmut_57': x_validate_logistic_parameters__mutmut_57, 
    'x_validate_logistic_parameters__mutmut_58': x_validate_logistic_parameters__mutmut_58, 
    'x_validate_logistic_parameters__mutmut_59': x_validate_logistic_parameters__mutmut_59, 
    'x_validate_logistic_parameters__mutmut_60': x_validate_logistic_parameters__mutmut_60, 
    'x_validate_logistic_parameters__mutmut_61': x_validate_logistic_parameters__mutmut_61, 
    'x_validate_logistic_parameters__mutmut_62': x_validate_logistic_parameters__mutmut_62, 
    'x_validate_logistic_parameters__mutmut_63': x_validate_logistic_parameters__mutmut_63, 
    'x_validate_logistic_parameters__mutmut_64': x_validate_logistic_parameters__mutmut_64, 
    'x_validate_logistic_parameters__mutmut_65': x_validate_logistic_parameters__mutmut_65, 
    'x_validate_logistic_parameters__mutmut_66': x_validate_logistic_parameters__mutmut_66, 
    'x_validate_logistic_parameters__mutmut_67': x_validate_logistic_parameters__mutmut_67, 
    'x_validate_logistic_parameters__mutmut_68': x_validate_logistic_parameters__mutmut_68, 
    'x_validate_logistic_parameters__mutmut_69': x_validate_logistic_parameters__mutmut_69, 
    'x_validate_logistic_parameters__mutmut_70': x_validate_logistic_parameters__mutmut_70, 
    'x_validate_logistic_parameters__mutmut_71': x_validate_logistic_parameters__mutmut_71, 
    'x_validate_logistic_parameters__mutmut_72': x_validate_logistic_parameters__mutmut_72, 
    'x_validate_logistic_parameters__mutmut_73': x_validate_logistic_parameters__mutmut_73, 
    'x_validate_logistic_parameters__mutmut_74': x_validate_logistic_parameters__mutmut_74, 
    'x_validate_logistic_parameters__mutmut_75': x_validate_logistic_parameters__mutmut_75, 
    'x_validate_logistic_parameters__mutmut_76': x_validate_logistic_parameters__mutmut_76, 
    'x_validate_logistic_parameters__mutmut_77': x_validate_logistic_parameters__mutmut_77, 
    'x_validate_logistic_parameters__mutmut_78': x_validate_logistic_parameters__mutmut_78, 
    'x_validate_logistic_parameters__mutmut_79': x_validate_logistic_parameters__mutmut_79, 
    'x_validate_logistic_parameters__mutmut_80': x_validate_logistic_parameters__mutmut_80, 
    'x_validate_logistic_parameters__mutmut_81': x_validate_logistic_parameters__mutmut_81, 
    'x_validate_logistic_parameters__mutmut_82': x_validate_logistic_parameters__mutmut_82, 
    'x_validate_logistic_parameters__mutmut_83': x_validate_logistic_parameters__mutmut_83, 
    'x_validate_logistic_parameters__mutmut_84': x_validate_logistic_parameters__mutmut_84, 
    'x_validate_logistic_parameters__mutmut_85': x_validate_logistic_parameters__mutmut_85, 
    'x_validate_logistic_parameters__mutmut_86': x_validate_logistic_parameters__mutmut_86, 
    'x_validate_logistic_parameters__mutmut_87': x_validate_logistic_parameters__mutmut_87, 
    'x_validate_logistic_parameters__mutmut_88': x_validate_logistic_parameters__mutmut_88, 
    'x_validate_logistic_parameters__mutmut_89': x_validate_logistic_parameters__mutmut_89, 
    'x_validate_logistic_parameters__mutmut_90': x_validate_logistic_parameters__mutmut_90, 
    'x_validate_logistic_parameters__mutmut_91': x_validate_logistic_parameters__mutmut_91, 
    'x_validate_logistic_parameters__mutmut_92': x_validate_logistic_parameters__mutmut_92, 
    'x_validate_logistic_parameters__mutmut_93': x_validate_logistic_parameters__mutmut_93, 
    'x_validate_logistic_parameters__mutmut_94': x_validate_logistic_parameters__mutmut_94
}
x_validate_logistic_parameters__mutmut_orig.__name__ = 'x_validate_logistic_parameters'


def validate_gompertz_parameters(params: dict[str, float]) -> dict[str, Any]:
    args = [params]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_validate_gompertz_parameters__mutmut_orig, x_validate_gompertz_parameters__mutmut_mutants, args, kwargs, None)


def x_validate_gompertz_parameters__mutmut_orig(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_1(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = None
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_2(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = None

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_3(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = False

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_4(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = None
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_5(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["XXaXX", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_6(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["A", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_7(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "XXbXX", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_8(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "B", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_9(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "XXcXX"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_10(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "C"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_11(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = None
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_12(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_13(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(None)
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_14(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = None

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_15(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = True

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_16(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "XXaXX" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_17(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "A" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_18(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" not in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_19(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = None
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_20(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(None, "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_21(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], None, min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_22(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=None)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_23(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float("a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_24(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_25(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", )
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_26(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["XXaXX"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_27(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["A"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_28(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "XXaXX", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_29(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "A", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_30(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=1.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_31(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a < 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_32(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 1:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_33(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(None)
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_34(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = None
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_35(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = True
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_36(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(None)
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_37(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['XXaXX']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_38(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['A']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_39(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = None

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_40(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = True

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_41(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "XXbXX" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_42(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "B" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_43(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" not in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_44(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = None
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_45(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(None, "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_46(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], None, min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_47(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=None)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_48(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float("b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_49(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_50(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", )
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_51(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["XXbXX"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_52(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["B"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_53(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "XXbXX", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_54(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "B", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_55(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=1.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_56(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b < 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_57(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 1:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_58(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(None)
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_59(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = None
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_60(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = True
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_61(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(None)
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_62(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['XXbXX']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_63(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['B']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_64(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = None

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_65(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = True

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_66(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "XXcXX" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_67(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "C" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_68(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" not in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_69(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = None
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_70(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(None, "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_71(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], None, min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_72(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=None, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_73(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=None)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_74(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float("c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_75(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_76(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_77(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, )
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_78(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["XXcXX"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_79(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["C"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_80(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "XXcXX", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_81(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "C", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_82(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=1.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_83(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=2.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_84(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c < 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_85(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 1:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_86(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(None)
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_87(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = None
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_88(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = True
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_89(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(None)
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_90(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['XXcXX']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_91(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['C']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_92(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = None

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_93(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = True

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_94(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "XXis_validXX": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_95(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "IS_VALID": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_96(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "XXissuesXX": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_97(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "ISSUES": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_98(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "XXrecommended_actionXX": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_99(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "RECOMMENDED_ACTION": "Adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_100(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "XXAdjust parameters to meet validation criteriaXX" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_101(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "adjust parameters to meet validation criteria" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_102(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "ADJUST PARAMETERS TO MEET VALIDATION CRITERIA" if not is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_103(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if is_valid else "No action needed",
    }


def x_validate_gompertz_parameters__mutmut_104(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "XXNo action neededXX",
    }


def x_validate_gompertz_parameters__mutmut_105(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "no action needed",
    }


def x_validate_gompertz_parameters__mutmut_106(params: dict[str, float]) -> dict[str, Any]:
    """
    Validate Gompertz model parameters to ensure they result in reasonable behavior.

    Parameters
    ----------
    params : Dict[str, float]
        Dictionary of model parameters

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    required_params = ["a", "b", "c"]
    missing_params = [p for p in required_params if p not in params]
    if missing_params:
        issues.append(f"Missing required parameters: {missing_params}")
        is_valid = False

    # Validate parameter values if they exist
    if "a" in params:
        try:
            a = validate_float(params["a"], "a", min_val=0.0)
            if a <= 0:
                issues.append(f"Parameter 'a' (upper asymptote) should be positive, got {a}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'a' must be a positive number, got {params['a']}")
            is_valid = False

    if "b" in params:
        try:
            b = validate_float(params["b"], "b", min_val=0.0)
            if b <= 0:
                issues.append(f"Parameter 'b' (displacement along y-axis) should be positive, got {b}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'b' must be a positive number, got {params['b']}")
            is_valid = False

    if "c" in params:
        try:
            c = validate_float(params["c"], "c", min_val=0.0, max_val=1.0)
            if c <= 0:
                issues.append(f"Parameter 'c' (growth rate) should be positive, got {c}")
                is_valid = False
        except (ValueError, TypeError):
            issues.append(f"Parameter 'c' must be a positive number between 0 and 1, got {params['c']}")
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust parameters to meet validation criteria" if not is_valid else "NO ACTION NEEDED",
    }

x_validate_gompertz_parameters__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_validate_gompertz_parameters__mutmut_1': x_validate_gompertz_parameters__mutmut_1, 
    'x_validate_gompertz_parameters__mutmut_2': x_validate_gompertz_parameters__mutmut_2, 
    'x_validate_gompertz_parameters__mutmut_3': x_validate_gompertz_parameters__mutmut_3, 
    'x_validate_gompertz_parameters__mutmut_4': x_validate_gompertz_parameters__mutmut_4, 
    'x_validate_gompertz_parameters__mutmut_5': x_validate_gompertz_parameters__mutmut_5, 
    'x_validate_gompertz_parameters__mutmut_6': x_validate_gompertz_parameters__mutmut_6, 
    'x_validate_gompertz_parameters__mutmut_7': x_validate_gompertz_parameters__mutmut_7, 
    'x_validate_gompertz_parameters__mutmut_8': x_validate_gompertz_parameters__mutmut_8, 
    'x_validate_gompertz_parameters__mutmut_9': x_validate_gompertz_parameters__mutmut_9, 
    'x_validate_gompertz_parameters__mutmut_10': x_validate_gompertz_parameters__mutmut_10, 
    'x_validate_gompertz_parameters__mutmut_11': x_validate_gompertz_parameters__mutmut_11, 
    'x_validate_gompertz_parameters__mutmut_12': x_validate_gompertz_parameters__mutmut_12, 
    'x_validate_gompertz_parameters__mutmut_13': x_validate_gompertz_parameters__mutmut_13, 
    'x_validate_gompertz_parameters__mutmut_14': x_validate_gompertz_parameters__mutmut_14, 
    'x_validate_gompertz_parameters__mutmut_15': x_validate_gompertz_parameters__mutmut_15, 
    'x_validate_gompertz_parameters__mutmut_16': x_validate_gompertz_parameters__mutmut_16, 
    'x_validate_gompertz_parameters__mutmut_17': x_validate_gompertz_parameters__mutmut_17, 
    'x_validate_gompertz_parameters__mutmut_18': x_validate_gompertz_parameters__mutmut_18, 
    'x_validate_gompertz_parameters__mutmut_19': x_validate_gompertz_parameters__mutmut_19, 
    'x_validate_gompertz_parameters__mutmut_20': x_validate_gompertz_parameters__mutmut_20, 
    'x_validate_gompertz_parameters__mutmut_21': x_validate_gompertz_parameters__mutmut_21, 
    'x_validate_gompertz_parameters__mutmut_22': x_validate_gompertz_parameters__mutmut_22, 
    'x_validate_gompertz_parameters__mutmut_23': x_validate_gompertz_parameters__mutmut_23, 
    'x_validate_gompertz_parameters__mutmut_24': x_validate_gompertz_parameters__mutmut_24, 
    'x_validate_gompertz_parameters__mutmut_25': x_validate_gompertz_parameters__mutmut_25, 
    'x_validate_gompertz_parameters__mutmut_26': x_validate_gompertz_parameters__mutmut_26, 
    'x_validate_gompertz_parameters__mutmut_27': x_validate_gompertz_parameters__mutmut_27, 
    'x_validate_gompertz_parameters__mutmut_28': x_validate_gompertz_parameters__mutmut_28, 
    'x_validate_gompertz_parameters__mutmut_29': x_validate_gompertz_parameters__mutmut_29, 
    'x_validate_gompertz_parameters__mutmut_30': x_validate_gompertz_parameters__mutmut_30, 
    'x_validate_gompertz_parameters__mutmut_31': x_validate_gompertz_parameters__mutmut_31, 
    'x_validate_gompertz_parameters__mutmut_32': x_validate_gompertz_parameters__mutmut_32, 
    'x_validate_gompertz_parameters__mutmut_33': x_validate_gompertz_parameters__mutmut_33, 
    'x_validate_gompertz_parameters__mutmut_34': x_validate_gompertz_parameters__mutmut_34, 
    'x_validate_gompertz_parameters__mutmut_35': x_validate_gompertz_parameters__mutmut_35, 
    'x_validate_gompertz_parameters__mutmut_36': x_validate_gompertz_parameters__mutmut_36, 
    'x_validate_gompertz_parameters__mutmut_37': x_validate_gompertz_parameters__mutmut_37, 
    'x_validate_gompertz_parameters__mutmut_38': x_validate_gompertz_parameters__mutmut_38, 
    'x_validate_gompertz_parameters__mutmut_39': x_validate_gompertz_parameters__mutmut_39, 
    'x_validate_gompertz_parameters__mutmut_40': x_validate_gompertz_parameters__mutmut_40, 
    'x_validate_gompertz_parameters__mutmut_41': x_validate_gompertz_parameters__mutmut_41, 
    'x_validate_gompertz_parameters__mutmut_42': x_validate_gompertz_parameters__mutmut_42, 
    'x_validate_gompertz_parameters__mutmut_43': x_validate_gompertz_parameters__mutmut_43, 
    'x_validate_gompertz_parameters__mutmut_44': x_validate_gompertz_parameters__mutmut_44, 
    'x_validate_gompertz_parameters__mutmut_45': x_validate_gompertz_parameters__mutmut_45, 
    'x_validate_gompertz_parameters__mutmut_46': x_validate_gompertz_parameters__mutmut_46, 
    'x_validate_gompertz_parameters__mutmut_47': x_validate_gompertz_parameters__mutmut_47, 
    'x_validate_gompertz_parameters__mutmut_48': x_validate_gompertz_parameters__mutmut_48, 
    'x_validate_gompertz_parameters__mutmut_49': x_validate_gompertz_parameters__mutmut_49, 
    'x_validate_gompertz_parameters__mutmut_50': x_validate_gompertz_parameters__mutmut_50, 
    'x_validate_gompertz_parameters__mutmut_51': x_validate_gompertz_parameters__mutmut_51, 
    'x_validate_gompertz_parameters__mutmut_52': x_validate_gompertz_parameters__mutmut_52, 
    'x_validate_gompertz_parameters__mutmut_53': x_validate_gompertz_parameters__mutmut_53, 
    'x_validate_gompertz_parameters__mutmut_54': x_validate_gompertz_parameters__mutmut_54, 
    'x_validate_gompertz_parameters__mutmut_55': x_validate_gompertz_parameters__mutmut_55, 
    'x_validate_gompertz_parameters__mutmut_56': x_validate_gompertz_parameters__mutmut_56, 
    'x_validate_gompertz_parameters__mutmut_57': x_validate_gompertz_parameters__mutmut_57, 
    'x_validate_gompertz_parameters__mutmut_58': x_validate_gompertz_parameters__mutmut_58, 
    'x_validate_gompertz_parameters__mutmut_59': x_validate_gompertz_parameters__mutmut_59, 
    'x_validate_gompertz_parameters__mutmut_60': x_validate_gompertz_parameters__mutmut_60, 
    'x_validate_gompertz_parameters__mutmut_61': x_validate_gompertz_parameters__mutmut_61, 
    'x_validate_gompertz_parameters__mutmut_62': x_validate_gompertz_parameters__mutmut_62, 
    'x_validate_gompertz_parameters__mutmut_63': x_validate_gompertz_parameters__mutmut_63, 
    'x_validate_gompertz_parameters__mutmut_64': x_validate_gompertz_parameters__mutmut_64, 
    'x_validate_gompertz_parameters__mutmut_65': x_validate_gompertz_parameters__mutmut_65, 
    'x_validate_gompertz_parameters__mutmut_66': x_validate_gompertz_parameters__mutmut_66, 
    'x_validate_gompertz_parameters__mutmut_67': x_validate_gompertz_parameters__mutmut_67, 
    'x_validate_gompertz_parameters__mutmut_68': x_validate_gompertz_parameters__mutmut_68, 
    'x_validate_gompertz_parameters__mutmut_69': x_validate_gompertz_parameters__mutmut_69, 
    'x_validate_gompertz_parameters__mutmut_70': x_validate_gompertz_parameters__mutmut_70, 
    'x_validate_gompertz_parameters__mutmut_71': x_validate_gompertz_parameters__mutmut_71, 
    'x_validate_gompertz_parameters__mutmut_72': x_validate_gompertz_parameters__mutmut_72, 
    'x_validate_gompertz_parameters__mutmut_73': x_validate_gompertz_parameters__mutmut_73, 
    'x_validate_gompertz_parameters__mutmut_74': x_validate_gompertz_parameters__mutmut_74, 
    'x_validate_gompertz_parameters__mutmut_75': x_validate_gompertz_parameters__mutmut_75, 
    'x_validate_gompertz_parameters__mutmut_76': x_validate_gompertz_parameters__mutmut_76, 
    'x_validate_gompertz_parameters__mutmut_77': x_validate_gompertz_parameters__mutmut_77, 
    'x_validate_gompertz_parameters__mutmut_78': x_validate_gompertz_parameters__mutmut_78, 
    'x_validate_gompertz_parameters__mutmut_79': x_validate_gompertz_parameters__mutmut_79, 
    'x_validate_gompertz_parameters__mutmut_80': x_validate_gompertz_parameters__mutmut_80, 
    'x_validate_gompertz_parameters__mutmut_81': x_validate_gompertz_parameters__mutmut_81, 
    'x_validate_gompertz_parameters__mutmut_82': x_validate_gompertz_parameters__mutmut_82, 
    'x_validate_gompertz_parameters__mutmut_83': x_validate_gompertz_parameters__mutmut_83, 
    'x_validate_gompertz_parameters__mutmut_84': x_validate_gompertz_parameters__mutmut_84, 
    'x_validate_gompertz_parameters__mutmut_85': x_validate_gompertz_parameters__mutmut_85, 
    'x_validate_gompertz_parameters__mutmut_86': x_validate_gompertz_parameters__mutmut_86, 
    'x_validate_gompertz_parameters__mutmut_87': x_validate_gompertz_parameters__mutmut_87, 
    'x_validate_gompertz_parameters__mutmut_88': x_validate_gompertz_parameters__mutmut_88, 
    'x_validate_gompertz_parameters__mutmut_89': x_validate_gompertz_parameters__mutmut_89, 
    'x_validate_gompertz_parameters__mutmut_90': x_validate_gompertz_parameters__mutmut_90, 
    'x_validate_gompertz_parameters__mutmut_91': x_validate_gompertz_parameters__mutmut_91, 
    'x_validate_gompertz_parameters__mutmut_92': x_validate_gompertz_parameters__mutmut_92, 
    'x_validate_gompertz_parameters__mutmut_93': x_validate_gompertz_parameters__mutmut_93, 
    'x_validate_gompertz_parameters__mutmut_94': x_validate_gompertz_parameters__mutmut_94, 
    'x_validate_gompertz_parameters__mutmut_95': x_validate_gompertz_parameters__mutmut_95, 
    'x_validate_gompertz_parameters__mutmut_96': x_validate_gompertz_parameters__mutmut_96, 
    'x_validate_gompertz_parameters__mutmut_97': x_validate_gompertz_parameters__mutmut_97, 
    'x_validate_gompertz_parameters__mutmut_98': x_validate_gompertz_parameters__mutmut_98, 
    'x_validate_gompertz_parameters__mutmut_99': x_validate_gompertz_parameters__mutmut_99, 
    'x_validate_gompertz_parameters__mutmut_100': x_validate_gompertz_parameters__mutmut_100, 
    'x_validate_gompertz_parameters__mutmut_101': x_validate_gompertz_parameters__mutmut_101, 
    'x_validate_gompertz_parameters__mutmut_102': x_validate_gompertz_parameters__mutmut_102, 
    'x_validate_gompertz_parameters__mutmut_103': x_validate_gompertz_parameters__mutmut_103, 
    'x_validate_gompertz_parameters__mutmut_104': x_validate_gompertz_parameters__mutmut_104, 
    'x_validate_gompertz_parameters__mutmut_105': x_validate_gompertz_parameters__mutmut_105, 
    'x_validate_gompertz_parameters__mutmut_106': x_validate_gompertz_parameters__mutmut_106
}
x_validate_gompertz_parameters__mutmut_orig.__name__ = 'x_validate_gompertz_parameters'


def validate_model_predictions(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    args = [model, t_pred, y_pred, max_growth_ratio]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_validate_model_predictions__mutmut_orig, x_validate_model_predictions__mutmut_mutants, args, kwargs, None)


def x_validate_model_predictions__mutmut_orig(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_1(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 1.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_2(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = None
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_3(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = None

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_4(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = False

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_5(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_6(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(None):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_7(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(None)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_8(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append(None)
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_9(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("XXModel predictions contain non-finite values (NaN or Inf)XX")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_10(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("model predictions contain non-finite values (nan or inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_11(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("MODEL PREDICTIONS CONTAIN NON-FINITE VALUES (NAN OR INF)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_12(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = None

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_13(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = True

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_14(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(None):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_15(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred <= 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_16(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 1):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_17(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(None)
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_18(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(None)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_19(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = None

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_20(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = True

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_21(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) >= 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_22(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 2:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_23(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = None
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_24(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(None)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_25(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = None

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_26(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(None)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_27(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = None
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_28(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(None)
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_29(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:+1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_30(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-2])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_31(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = None  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_32(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(None, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_33(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, None, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_34(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, None)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_35(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_36(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_37(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, )  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_38(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals != 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_39(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 1, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_40(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1.0000000001, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_41(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = None

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_42(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs * prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_43(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(None):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_44(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth >= max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_45(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = None
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_46(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(None)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_47(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                None
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_48(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx - 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_49(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 2]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_50(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = None

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_51(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = True

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_52(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") or model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_53(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(None, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_54(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, None) and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_55(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr("monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_56(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, ) and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_57(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "XXmonotonic_checkXX") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_58(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "MONOTONIC_CHECK") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_59(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_60(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(None):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_61(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(None) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_62(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) > -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_63(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= +1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_64(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1.0000000001):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_65(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                None
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_66(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "XXModel predictions are not monotonically increasing (cumulative models should generally increase)XX"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_67(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_68(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "MODEL PREDICTIONS ARE NOT MONOTONICALLY INCREASING (CUMULATIVE MODELS SHOULD GENERALLY INCREASE)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_69(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = None

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_70(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = True

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_71(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "XXis_validXX": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_72(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "IS_VALID": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_73(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "XXissuesXX": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_74(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "ISSUES": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_75(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "XXrecommended_actionXX": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_76(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "RECOMMENDED_ACTION": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_77(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "XXAdjust model parameters or check for numerical issuesXX"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_78(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "adjust model parameters or check for numerical issues"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_79(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "ADJUST MODEL PARAMETERS OR CHECK FOR NUMERICAL ISSUES"
        if not is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_80(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if is_valid
        else "No action needed",
    }


def x_validate_model_predictions__mutmut_81(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "XXNo action neededXX",
    }


def x_validate_model_predictions__mutmut_82(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "no action needed",
    }


def x_validate_model_predictions__mutmut_83(
    model, t_pred: np.ndarray, y_pred: np.ndarray, max_growth_ratio: float = 0.5
) -> dict[str, Any]:
    """
    Validate that model predictions are reasonable.

    Parameters
    ----------
    model : DiffusionModel
        The fitted model
    t_pred : np.ndarray
        Time points
    y_pred : np.ndarray
        Predicted values
    max_growth_ratio : float
        Maximum allowed ratio of growth between consecutive points relative to current level

    Returns
    -------
    Dict[str, Any]
        Validation results with 'is_valid' flag and 'issues' list
    """
    issues = []
    is_valid = True

    # Check for non-finite values
    if not np.all(np.isfinite(y_pred)):
        issues.append("Model predictions contain non-finite values (NaN or Inf)")
        is_valid = False

    # Check for negative values (unusual for cumulative adoption)
    if np.any(y_pred < 0):
        issues.append(f"Model predictions contain negative values: min = {np.min(y_pred)}")
        is_valid = False

    # Check for reasonable growth rates
    if len(y_pred) > 1:
        diffs = np.diff(y_pred)
        abs_diffs = np.abs(diffs)

        # Calculate relative growth rate (avoid division by zero)
        prev_vals = np.abs(y_pred[:-1])
        prev_vals = np.where(prev_vals == 0, 1e-10, prev_vals)  # Avoid division by zero
        rel_growth = abs_diffs / prev_vals

        if np.any(rel_growth > max_growth_ratio):
            max_idx = np.argmax(rel_growth)
            issues.append(
                f"Unusually high growth rate detected: {rel_growth[max_idx]:.3f} "
                f"between t={t_pred[max_idx]} and t={t_pred[max_idx + 1]}"
            )
            is_valid = False

    # Check if predictions are monotonically increasing (for cumulative models)
    if hasattr(model, "monotonic_check") and model.monotonic_check:
        if not np.all(np.diff(y_pred) >= -1e-10):  # Allow small numerical errors
            issues.append(
                "Model predictions are not monotonically increasing (cumulative models should generally increase)"
            )
            is_valid = False

    return {
        "is_valid": is_valid,
        "issues": issues,
        "recommended_action": "Adjust model parameters or check for numerical issues"
        if not is_valid
        else "NO ACTION NEEDED",
    }

x_validate_model_predictions__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_validate_model_predictions__mutmut_1': x_validate_model_predictions__mutmut_1, 
    'x_validate_model_predictions__mutmut_2': x_validate_model_predictions__mutmut_2, 
    'x_validate_model_predictions__mutmut_3': x_validate_model_predictions__mutmut_3, 
    'x_validate_model_predictions__mutmut_4': x_validate_model_predictions__mutmut_4, 
    'x_validate_model_predictions__mutmut_5': x_validate_model_predictions__mutmut_5, 
    'x_validate_model_predictions__mutmut_6': x_validate_model_predictions__mutmut_6, 
    'x_validate_model_predictions__mutmut_7': x_validate_model_predictions__mutmut_7, 
    'x_validate_model_predictions__mutmut_8': x_validate_model_predictions__mutmut_8, 
    'x_validate_model_predictions__mutmut_9': x_validate_model_predictions__mutmut_9, 
    'x_validate_model_predictions__mutmut_10': x_validate_model_predictions__mutmut_10, 
    'x_validate_model_predictions__mutmut_11': x_validate_model_predictions__mutmut_11, 
    'x_validate_model_predictions__mutmut_12': x_validate_model_predictions__mutmut_12, 
    'x_validate_model_predictions__mutmut_13': x_validate_model_predictions__mutmut_13, 
    'x_validate_model_predictions__mutmut_14': x_validate_model_predictions__mutmut_14, 
    'x_validate_model_predictions__mutmut_15': x_validate_model_predictions__mutmut_15, 
    'x_validate_model_predictions__mutmut_16': x_validate_model_predictions__mutmut_16, 
    'x_validate_model_predictions__mutmut_17': x_validate_model_predictions__mutmut_17, 
    'x_validate_model_predictions__mutmut_18': x_validate_model_predictions__mutmut_18, 
    'x_validate_model_predictions__mutmut_19': x_validate_model_predictions__mutmut_19, 
    'x_validate_model_predictions__mutmut_20': x_validate_model_predictions__mutmut_20, 
    'x_validate_model_predictions__mutmut_21': x_validate_model_predictions__mutmut_21, 
    'x_validate_model_predictions__mutmut_22': x_validate_model_predictions__mutmut_22, 
    'x_validate_model_predictions__mutmut_23': x_validate_model_predictions__mutmut_23, 
    'x_validate_model_predictions__mutmut_24': x_validate_model_predictions__mutmut_24, 
    'x_validate_model_predictions__mutmut_25': x_validate_model_predictions__mutmut_25, 
    'x_validate_model_predictions__mutmut_26': x_validate_model_predictions__mutmut_26, 
    'x_validate_model_predictions__mutmut_27': x_validate_model_predictions__mutmut_27, 
    'x_validate_model_predictions__mutmut_28': x_validate_model_predictions__mutmut_28, 
    'x_validate_model_predictions__mutmut_29': x_validate_model_predictions__mutmut_29, 
    'x_validate_model_predictions__mutmut_30': x_validate_model_predictions__mutmut_30, 
    'x_validate_model_predictions__mutmut_31': x_validate_model_predictions__mutmut_31, 
    'x_validate_model_predictions__mutmut_32': x_validate_model_predictions__mutmut_32, 
    'x_validate_model_predictions__mutmut_33': x_validate_model_predictions__mutmut_33, 
    'x_validate_model_predictions__mutmut_34': x_validate_model_predictions__mutmut_34, 
    'x_validate_model_predictions__mutmut_35': x_validate_model_predictions__mutmut_35, 
    'x_validate_model_predictions__mutmut_36': x_validate_model_predictions__mutmut_36, 
    'x_validate_model_predictions__mutmut_37': x_validate_model_predictions__mutmut_37, 
    'x_validate_model_predictions__mutmut_38': x_validate_model_predictions__mutmut_38, 
    'x_validate_model_predictions__mutmut_39': x_validate_model_predictions__mutmut_39, 
    'x_validate_model_predictions__mutmut_40': x_validate_model_predictions__mutmut_40, 
    'x_validate_model_predictions__mutmut_41': x_validate_model_predictions__mutmut_41, 
    'x_validate_model_predictions__mutmut_42': x_validate_model_predictions__mutmut_42, 
    'x_validate_model_predictions__mutmut_43': x_validate_model_predictions__mutmut_43, 
    'x_validate_model_predictions__mutmut_44': x_validate_model_predictions__mutmut_44, 
    'x_validate_model_predictions__mutmut_45': x_validate_model_predictions__mutmut_45, 
    'x_validate_model_predictions__mutmut_46': x_validate_model_predictions__mutmut_46, 
    'x_validate_model_predictions__mutmut_47': x_validate_model_predictions__mutmut_47, 
    'x_validate_model_predictions__mutmut_48': x_validate_model_predictions__mutmut_48, 
    'x_validate_model_predictions__mutmut_49': x_validate_model_predictions__mutmut_49, 
    'x_validate_model_predictions__mutmut_50': x_validate_model_predictions__mutmut_50, 
    'x_validate_model_predictions__mutmut_51': x_validate_model_predictions__mutmut_51, 
    'x_validate_model_predictions__mutmut_52': x_validate_model_predictions__mutmut_52, 
    'x_validate_model_predictions__mutmut_53': x_validate_model_predictions__mutmut_53, 
    'x_validate_model_predictions__mutmut_54': x_validate_model_predictions__mutmut_54, 
    'x_validate_model_predictions__mutmut_55': x_validate_model_predictions__mutmut_55, 
    'x_validate_model_predictions__mutmut_56': x_validate_model_predictions__mutmut_56, 
    'x_validate_model_predictions__mutmut_57': x_validate_model_predictions__mutmut_57, 
    'x_validate_model_predictions__mutmut_58': x_validate_model_predictions__mutmut_58, 
    'x_validate_model_predictions__mutmut_59': x_validate_model_predictions__mutmut_59, 
    'x_validate_model_predictions__mutmut_60': x_validate_model_predictions__mutmut_60, 
    'x_validate_model_predictions__mutmut_61': x_validate_model_predictions__mutmut_61, 
    'x_validate_model_predictions__mutmut_62': x_validate_model_predictions__mutmut_62, 
    'x_validate_model_predictions__mutmut_63': x_validate_model_predictions__mutmut_63, 
    'x_validate_model_predictions__mutmut_64': x_validate_model_predictions__mutmut_64, 
    'x_validate_model_predictions__mutmut_65': x_validate_model_predictions__mutmut_65, 
    'x_validate_model_predictions__mutmut_66': x_validate_model_predictions__mutmut_66, 
    'x_validate_model_predictions__mutmut_67': x_validate_model_predictions__mutmut_67, 
    'x_validate_model_predictions__mutmut_68': x_validate_model_predictions__mutmut_68, 
    'x_validate_model_predictions__mutmut_69': x_validate_model_predictions__mutmut_69, 
    'x_validate_model_predictions__mutmut_70': x_validate_model_predictions__mutmut_70, 
    'x_validate_model_predictions__mutmut_71': x_validate_model_predictions__mutmut_71, 
    'x_validate_model_predictions__mutmut_72': x_validate_model_predictions__mutmut_72, 
    'x_validate_model_predictions__mutmut_73': x_validate_model_predictions__mutmut_73, 
    'x_validate_model_predictions__mutmut_74': x_validate_model_predictions__mutmut_74, 
    'x_validate_model_predictions__mutmut_75': x_validate_model_predictions__mutmut_75, 
    'x_validate_model_predictions__mutmut_76': x_validate_model_predictions__mutmut_76, 
    'x_validate_model_predictions__mutmut_77': x_validate_model_predictions__mutmut_77, 
    'x_validate_model_predictions__mutmut_78': x_validate_model_predictions__mutmut_78, 
    'x_validate_model_predictions__mutmut_79': x_validate_model_predictions__mutmut_79, 
    'x_validate_model_predictions__mutmut_80': x_validate_model_predictions__mutmut_80, 
    'x_validate_model_predictions__mutmut_81': x_validate_model_predictions__mutmut_81, 
    'x_validate_model_predictions__mutmut_82': x_validate_model_predictions__mutmut_82, 
    'x_validate_model_predictions__mutmut_83': x_validate_model_predictions__mutmut_83
}
x_validate_model_predictions__mutmut_orig.__name__ = 'x_validate_model_predictions'
