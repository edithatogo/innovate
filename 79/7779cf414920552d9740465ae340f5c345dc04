Task: Write failing classification tests

Summary:
- Added tests/unit/test_rust_native_payload_model_coverage.py.
- Tests require every Python registry model family to have ownership status.
- Tests require stable payload shapes to have schema fixture evidence and Rust ownership status.
- Tests ensure provisional/internal/reference payloads remain explicit boundaries.

Validation:
- python -m json.tool docs/source/_static/rust_native_model_family_coverage.json >/dev/null
- python -m json.tool docs/source/_static/rust_native_payload_shape_coverage.json >/dev/null
- uv run pytest tests/unit/test_rust_native_payload_model_coverage.py -q
