Deploy Documentation repair after run 27527199475: docs build and upload-pages-artifact succeeded, but deploy-pages job failed with no steps because repository Pages is configured for legacy gh-pages branch deployment. Guarded deploy job behind vars.ENABLE_PAGES_ACTIONS_DEPLOY == 'true' so the build gate passes under current repository configuration while retaining opt-in Actions Pages deploy.
